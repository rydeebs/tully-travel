# Server-side only. Never exposed to the browser.
ANTHROPIC_API_KEY=
# Optional: override the model (default claude-opus-5)
# ANTHROPIC_MODEL=claude-opus-5
# Optional: port (default 5173)
# PORT=5173
